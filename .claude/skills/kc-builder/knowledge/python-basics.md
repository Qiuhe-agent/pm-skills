# Python 基础

## 变量和数据类型

Python 是动态类型语言，不需要声明变量类型。

```python
name = "Alice"
age = 25
is_student = True
```

## 列表

列表是有序的可变序列：

```python
fruits = ["apple", "banana", "cherry"]
fruits.append("date")
```

## 函数

使用 def 关键字定义函数：

```python
def greet(name):
    return f"Hello, {name}!"
```
